# Rubin Browser — Canonical TODO

Single source of truth for actionable items. **IDs are globally unique and never
reused or renumbered** — reference an item by its ID (e.g. "do 100–104"). Detailed
rationale lives in `BACKLOG.md`; this file is the numbered index + status.

Status: `TODO` · `WIP` (in progress) · `DONE` · `BLOCKED` (needs a decision — see note).

## Active batch (being implemented now)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| 100 | Plain-language glossary — hover tooltips + "?" panel | DONE | glossary section + filter in HelpModal; title= tooltips on DP1/Filter labels; module `glossary.ts` (44 terms) + HelpModal glossary tests |
| 101 | Catalog overlay + linked table (Gaia / Rubin Object via TAP) | DONE | Gaia DR3 overlay (public, no token): markers on the canvas + CatalogTable (row⇄marker select, recenter on click) over gaia.ts→catalog.ts; CatalogTable presentational + Playwright wiring test. Rubin Object source is a follow-up (needs tap.ts, now fixed). |
| 102 | MOC / DP1 footprint coverage layer (shade where data exists) | DONE | ImageViewer.renderCoverage() projects footprint.ts discs on the main canvas; ⊙ DP1 coverage toggle; Playwright paint/clear outcome test |
| 103 | Simbad "what's here?" (right-click) + name resolve | DONE | right-click → onSkyContext → objectsNear → SimbadPanel; public (no auth), works without a token; presentational + page.route Playwright tests |
| 104 | Click-to-copy coordinates (sexagesimal ⟷ decimal) | DONE | StatusBar: click RA/Dec to copy, format toggle, copy-pair (decimal); 4 tests |
| 105 | Grid coordinate-system toggle (equatorial ↔ galactic ↔ ecliptic) | DONE | graticuleLines generalized (equatorial path unchanged) via coords.ts; formatGridLabel (l/b, λ/β); System selector; iso-line + label unit tests + Playwright repaint test |
| 106 | PNG screenshot export of the current view | DONE | ImageViewer.exportPng() composites stacked canvases → toBlob download; ⤓ PNG button; Playwright download-outcome test |
| 107 | Magnifier loupe + whole-sky locator inset | DONE | loupe canvas magnifies pixels under the cursor (locator inset already existed as .fov-minimap); 🔍 Loupe toggle; Playwright tracks-cursor outcome test |
| 108 | Offline image-diff display (epoch A vs B → transients) | DONE | DiffPanel: A/B/diff (diverging) canvases + transient list over the tested offlineIntensityFrame→differenceImages(median)→detectTransients pipeline; ⧉ Diff toggle (offline only); ground-truth unit test + presentational + Playwright wiring test |
| 109 | FITS cutout display pipeline (SODA→FITS→WCS readout) | DONE | End-to-end: soda.ts (ObsTAP/DataLink discovery + SODA sync fetch) → readFits → wcs.ts (pure TAN/CD pixel↔sky) → cutoutRender.ts (percentile-clip→scaling→colormap, NaN→transparent) → CutoutPanel.svelte (canvas + scale/colormap/invert + live pixel-value & RA/Dec readout), wired into TileViewer via a 🔬 Cutout toggle with explicit idle/loading/loaded/error state (thrown errors shown verbatim, never a blank panel). Tested: wcs (18) + cutoutRender (6) + CutoutPanel (6) unit; Playwright cutout.spec routes a synthetic FITS + one ObsCore row and asserts non-black canvas + plausible RA/Dec, plus a zero-rows "No DP1 … covers" error-path. Scoped out: only linear TAN/CD WCS (no SIP/TPV distortion); the LIVE SODA path needs a real RSP token so it stays unverified in CI (endpoint + DataLink-ID recipe are documented assumptions in soda.ts/obscore.ts). |

## Backlog (not yet called out to implement)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| 120 | RGB band-mixing composite (ugrizy → R/G/B, per-channel stretch, Lupton) | DONE | Pure `luptonRgb.ts` (Float64 r/g/b → RGBA) implements the VERIFIED astropy `make_lupton_rgb` recipe: subtract per-channel minimum → I=(r+g+b)/3 → factor = asinh(I·Q/stretch)·slope/I with slope=0.1/asinh(0.1·Q), the I≤0 guard, clip-neg, and hue-preserving saturation (÷max), NaN-in-any-band → transparent. New `RgbCompositePanel.svelte` (band→channel selectors default i→R r→G g→B, Q/stretch sliders, honest differing-size error) + a 🌈 RGB toggle in TileViewer that fetches 3 per-band SODA cutouts (i,r,g) at the SAME ra/dec/size and composites; offline/no-token surfaces the thrown "requires sign-in" verbatim. Tests: adversarial `luptonRgb.test.ts` (channel-dominance R/B, neutral gray, NaN transparency, stretch↓/Q↑ brightens faint, minimum, size/param guards — a grayscale/swap/constant impl fails) + `RgbCompositePanel.test.ts` + Playwright `rgb-composite.spec.ts` (routes 3 per-band FITS by lsst_band; asserts COLOURED red-dominant pixels + mapping-swap changes them). NOTE: astropy `stretch` is a *linear* stretch — raising it DIMS the faint end (task's parenthetical was inverted); the test asserts the verified direction. Live multi-band SODA needs a real token (unverified in CI). |
| 121 | DS9 region files (draw / import / export circles·ellipses·polygons) | DONE | Pure `src/utils/ds9Regions.ts` (`parseDs9`/`serializeDs9`) — format verified against the authoritative SAOImageDS9 Regions reference (ds9.si.edu/doc/ref/region.html): header `# Region file format: DS9`, a coordinate-system line (fk5/fk4/icrs/j2000/image/…) applying to following shapes, BOTH paren-comma `circle(x,y,r)` and space forms, shapes circle/ellipse/box/polygon (point/line/annulus/text/… SKIPPED not thrown). Sky stored in ICRS DEGREES internally; length suffixes converted at the boundary (`"`=arcsec/3600, `'`=arcmin/60, `d`=deg, `r`=rad, bare sky=deg, image=px); sexagesimal RA in HOURS (odd arg ×15) / Dec in degrees; malformed line skipped (never throws); byte-idempotent round-trip. UI: ImageViewer gained a region overlay canvas (mirrors ruler/cross-section — steals the pointer only in region mode so pan is structurally suspended) — draw a circle (drag centre→radius = great-circle sep) or polygon (click vertices, click the first/Esc to close); regions stored as RA/Dec (+radius deg) and reprojected every frame via the real skyToCanvas so they track pan/zoom (committed layer always visible, drawn on the untainted overlay so it's in the PNG export too). TileViewer: ⬡ Regions toggle + shape selector + ⭳ Export .reg (serializeDs9→Blob download) + ⭱ Import (paste textarea / file input → parseDs9, honest "no recognizable regions" message on junk, never a crash) + Clear. Fixed a real stacking bug the tests caught (active full-viewer overlay z-index 7 covered the toolbar z-index 6 → toolbar unclickable while any pointer-tool active; bumped toolbar to 12). Tests: adversarial `ds9Regions.test.ts` (14 — real fk5 file: exact types + degrees + arcsec→deg converted so a unit-ignoring/shape-dropping impl fails; arcmin/rad/bare; sexagesimal hours×15; space form + box; unknown skipped; malformed no-throw; junk→[]; round-trip numeric + byte-idempotent; ellipse/box serialize; mixed-frame inline) + Playwright `ds9-regions.spec.ts` (3 — draw circle PAINTS via getImageData AND survives+repositions on a pan proving sky-anchored; Export download text contains `circle(`; Import pasted fk5 circle renders). ds9Regions.ts coverage 92.5/86.8/100/99.2. Scoped out: image-frame regions parse+round-trip but aren't drawn (no WCS to map pixels→sky); galactic/ecliptic values read best-effort under icrs. |
| 122 | Aperture / radial-profile photometry (click → profile, curve-of-growth) | DONE | Pure `photometry.ts`: `aperturePhotometry(image,cx,cy,{rAper,rInner,rOuter})` (sum r≤rAper, MEDIAN-annulus background, net = apΣ − bkg·nPix, rough SNR from annulus RMS) + `radialProfile(image,cx,cy,{maxRadius,nBins})` (per-annulus mean + cumulative encircled-flux curve-of-growth). Pixel-centre convention matches wcs.ts (cx,cy are 1-based FITS px=col+1,py=row+1); NaN + edge pixels excluded (never NaN-poisoned). CutoutPanel gained a ⊕ Measure mode: click → aperture+annulus SVG overlay (data-pixel viewBox) + rAper slider + net-flux/bkg/SNR readout (with BUNIT) + `RadialProfilePlot.svelte` (SVG growth + mean curves). Tests: adversarial `photometry.test.ts` against a 2D Gaussian of KNOWN total (A·2πσ²) — curve-of-growth monotonic non-decreasing & encircled→total within 2%; flat field nets ~0; nonzero-sky pedestal subtracted; NaN/edge excluded; guards (a wrong-area / background-sign / no-accumulate impl fails) + `RadialProfilePlot.test.ts` + CutoutPanel measure test + Playwright `photometry.spec.ts` (routes a Gaussian-source FITS; click centre → aperture overlay drawn + non-empty growth polyline + positive net flux). |
| 123 | Object-type classification FROM the image under the cursor | TODO | BACKLOG #10 (PRD ready, measured accuracy required) |
| 124 | Time-varying intensity across multi-epoch cubes (multi-scale, sub-pixel) | DONE (analysis) | Pure `variability.ts`: `variabilityStats` (mean/rms/mad/amplitude/peakIndex + inverse-variance reduced-chi²), `variabilityMap` (per-pixel index; default temporal-std so a bright CONSTANT source sits at the noise floor while a 1-2-epoch transient surfaces), `multiScaleVariability` (box-bin per scale so point AND extended variability surface; integer bins, sub-pixel out of scope), `detectVariableSources` (median+nSigma·MAD threshold → connected components → min-sep dedup, NaN-safe). Mutation-tested against the offline cube's KNOWN transient/constant ground truth; 21 unit tests. DISPLAY (variability-map overlay / variable list in TileViewer) is a deliberate follow-up. |
| 125 | Zoom/fetch perf tail: off-thread `createImageBitmap` decode, request cancellation, higher-res allsky | DONE | Shipped: **in-flight DEDUP** (`tileScheduler.ts` — a tile requested twice before it resolves hits the network once), **request CANCELLATION** (superseded batches abort in-flight loads — `AbortController` for the fetch paths, detach handlers + `src=''` for `<img>`; a cancelled tile re-fetches when it reappears; an abort never counts as a tile failure / never trips auto-fallback), the **higher-res HiPS Allsky backdrop** (fetch the single `Norder3/Allsky.<ext>` preview instead of 48 order-1 tiles; slice its 768 order-3 tiles — IVOA packing `n_tiles_in_row=int(sqrt(768))=27`, `divmod(ipix,27)` — into a SEPARATE backdrop map drawn as Pass 0; graceful fallback to the order-1 enumeration on 404/taint), and now **off-thread `createImageBitmap` decode**: tiles load `fetch(CORS)→blob→createImageBitmap(blob)` (public + auth network paths) and offline synthesises `createImageBitmap(ImageData)` directly (no `toDataURL` PNG round-trip), caching an `ImageBitmap` the renderer draws unchanged. The blob is always CORS-readable (never `no-cors`) so the canvas is NOT tainted — `getImageData` (post-processing, cross-section, readout, surface) still works. Fallbacks preserve behaviour: no `createImageBitmap` (jsdom/old) or a decode throw / CORS-opaque fetch → the `<img crossOrigin>` path. Bitmaps are `close()`d on LRU eviction (never a visible one). The network slot frees at BLOB time so decode runs off the fetch queue; the public path issues `fetch` immediately (browser per-host cap throttles it, as `<img>` did) — auth stays queued. New pure `decodedTile.ts` (`tileReady`/`tileWidth`/`closeTile`, duck-typed on `naturalWidth`). Tests: `decodedTile.test.ts` + `evictLru` onEvict unit tests; `tile-decode.spec.ts` PROVES the off-thread path is taken (`__tileDecodeCounts.bitmap>0`) on DSS + offline and that `getImageData` is NOT tainted + cross-section reads pixels; full visual/interaction/orientation/perf-hud gate + UNCHANGED screenshot baseline. |
| 126 | Real-data TAP regression fixtures (`tests/regression/` + `tests/fixtures/`) | DONE | Built the missing `tests/regression/` + `tests/fixtures/` dirs (the `test:regression` script targeted a non-existent path). `tests/fixtures/gaia-dr3lite-cone.json` is a VERBATIM live GAVO `gaia.dr3lite` response; `gaia.regression.test.ts` parses it via the real `JSON.parse`→`parseGaiaResponse` path and locks the true wire shape (the `columns` key not ESA's `metadata`, the server-derived `bp_rp` alias, null→NaN) — the class a hand mock hid in the Gaia CORS migration. Surfaced item 134. |
| 134 | Big-int-safe Gaia `source_id` (precision loss) | TODO | GAVO transmits `source_id` as a bare JSON NUMBER and every Gaia id > 2^53, so `JSON.parse` rounds it before `parseGaiaResponse` sees it → the stored id has wrong low digits. Fix needs a big-int-aware parse (parse source_id from raw text, or switch that column to VOTable/string). Pinned by a regression assertion in `gaia.regression.test.ts` that will flip when fixed. |
| 127 | Cross-platform polish + PWA (pinch-zoom, responsive breakpoints, manifest/SW) | TODO | BACKLOG #8 |
| 128 | Live Rubin DP1 Object cone search + catalog source + auth endpoint reconciliation | DONE | New `src/api/rubinObjects.ts`: pure `buildObjectConeSearch` emits `SELECT TOP N objectId, coord_ra, coord_dec, refBand, {ugrizy}_psfMag, r_cModelMag FROM dp1.Object WHERE CONTAINS(POINT('ICRS', coord_ra, coord_dec), CIRCLE('ICRS', ra, dec, radiusDeg)) = 1` — columns VERIFIED against the published sdm_schemas `dp1.yaml` (coord_ra/coord_dec are the canonical index columns, NOT the per-band z_ra/z_dec; per-band `_psfMag`/`_cModelMag` are AB mags), radius in DEGREES at the boundary, no SELECT */ORDER BY/dp02, finite-checked. `fetchRubinObjects` is auth-gated (throws the honest "sign in with an RSP token that has DP1 data rights" BEFORE any request), runs `tap.query()`, maps rows → the generic `CatalogSet` via pure `parseRubinObjects` (label = objectId kept as an exact decimal string; null band mag → dash; empty field → count-0 catalog, never fabricated rows; 401/404/network → distinct honest throws). Wired a "✚ Rubin Obj (DP1)" toggle in TileViewer alongside Gaia: an INDEPENDENT ImageViewer overlay (magenta square markers, distinct from Gaia round / lens diamond) + linked CatalogTable (row⇄marker select, recenter on click); off a Rubin base / unauthenticated it surfaces the honest prerequisite (mirrors the light-curve toggle), never a silent no-op. `auth.validateToken` reconciliation confirmed: it already targets Gafaelfawr `/auth/api/v1/user-info` (not the old `/api/dp1/query`), now pinned by an explicit negative assertion. Tests: adversarial `rubinObjects.test.ts` (21 — ADQL invariants, unauth-throws-before-query, fixture→exact CatalogSet, empty→0, 401/404/network); Playwright `rubin-objects.spec.ts` (routes the /rsp-proxied TAP + a token → markers PAINT + table lists the object + row click links/recenters; and the unauth/DSS prerequisite path with NO TAP call). LIVE DP1 path is token-gated / CI-unverified (no real RSP token) — verified via routed mocks + honest token-gated errors, same as 109/DIA. |
| 129 | DP1 dataset discovery: fetch `/api/hips/v2/dp1/list` instead of hardcoding datasets | TODO | BACKLOG B-a remainder |
| 130 | Gravitational-lens catalog overlay | DONE | Bundled, web-verified (SIMBAD J2000 + discovery papers) set of 19 famous strong lenses spanning all 4 types (`lenses.ts`→`lensCatalogSet()`→feature-101 `CatalogSet`). New INDEPENDENT ImageViewer layer (`renderLensCatalog`, gold labelled diamond markers) that coexists with the Gaia cyan layer (no silent conflict); ⬦ Lenses toggle + linked CatalogTable; row click recenters + reports name/type/z. Fixed a sign error in the Einstein Cross anchor (Dec is +3.3585, not −3.3586). Tests: unit `lenses.test.ts` asserts 5 anchors by name (fails on zeros/sign-flip); Playwright `lens-catalog.spec.ts` asserts markers paint + view recenters onto the lens. |
| 132 | Performance monitoring HUD + instrumentation | DONE | Pure `utils/perfMetrics.ts` (`PerfMetrics`): counters (tiles requested / fetched-from-network / served-from-cache / in-flight / errors), cache **hit-rate** = hits/(hits+misses) with a 0/0 guard, rolling min/max/avg windows for per-tile load time and per-frame render time (→ FPS), injectable clock. Wired into `ImageViewer`'s REAL fetch path (start/end/cache-hit/cancel/error) and render path (each full `render()` timed). New default-OFF `PerfHud.svelte` overlay (`aria-label="Performance HUD"`, stable per-field aria-labels) toggled from `TileViewer` (⏱ Perf), fed a throttled live snapshot via `onPerfSnapshot`. Fixed a real leak: the mount `$effect` cleanup aborted in-flight loads via `tileScheduler.clear()` without reconciling the perf gauge, so `in-flight` never returned to 0 on an effect re-run — added `abortInFlightLoads()` (clear + `recordFetchCancel` per aborted load). Tests: adversarial `perfMetrics.test.ts` (hit-rate formula + 0/0, rolling window bounding, FPS from render times, counter exactness — a constant stub fails); Playwright `perf-hud.spec.ts` (HUD toggles, metrics non-zero + sane, in-flight→0 at idle, cache hit-rate RISES on pan-away-and-back). |
| 133 | 3D surface = cross-section line over TIME (temporal waterfall) | DONE | User correction: the 3D surface must be the line cross-section over TIME, not a spatial patch. The surface is now `grid[epoch][position]` — col = position along the drawn cross-section line, row = epoch (row 0 = earliest, drawn at back → latest in front), height = intensity (normalised 0..1; NaN = gap). New PURE `temporalCrossSectionGrid(endpoints, epochCount, sampleCount, intensityAtEpoch)` in `crossSection.ts` (owns short-path-RA line interpolation + time ordering + normalisation). `sampleProfile` now carries the line's sky `endpoints` on its `LineProfile`, so `TileViewer` can re-walk the SAME on-sky line across every offline epoch WITHOUT touching `ImageViewer`: it derives `surfaceGrid` by sampling `offlineLightCurve(ra,dec,band)` (synthetic ground-truth `intensityAt`) per position × epoch. Meaningful ONLY over the OFFLINE multi-epoch cube (DP1 HiPS is a single coadd — no time axis), so it draws only when offline + a line is drawn + ≥2 epochs; otherwise an honest prompt ("Switch to the Offline demo layer…" / "Draw a cross-section line…"), never the old spatial relief, never a fabricated surface. `SurfacePlot` gained title/axis-label/caption/empty-message props (position × time axes). Scoped out: no live Rubin multi-epoch source (DP1 is a single deep coadd); uses ground-truth `offlineLightCurve` rather than rasterising each epoch's tile frame (the sky→canvas→tile mapping lives in off-limits ImageViewer, and the light curve is the exact signal those frames are built from). Tests: adversarial `temporalCrossSectionGrid` unit (peak COLUMN shifts between epoch ROWS — fails for a static/spatial/transposed impl; NaN propagation; row order = time; normalisation; fixed on-sky columns; RA-wrap) + Playwright `surface-temporal.spec.ts` (surface TRACKS the drawn line — a spatial relief would not — plus the honest empty state) and updated `surface-plot.spec.ts`. |
| 131 | Gaia rich visualization (colour/PM/parallax + linked CMD) | DONE | Surfaces the rich Gaia columns `gaia.ts` already fetched but the feature-101 overlay threw away, at zero new API cost. New pure `utils/gaiaViz.ts` (verified conventions: `pmra`=μα*=μα·cosδ; M_G=G+5·log10(ϖ_mas)−10 for ϖ>0; d_pc=1000/ϖ; blue→white→red BP−RP ramp, NaN→grey). `catalog.ts` `gaiaToCatalogSet` now carries optional `colorRgb`/`pmRaMasYr`/`pmDecMasYr` + a "Distance (pc)" column (— for ϖ≤0). `ImageViewer.renderCatalog` paints filled BP−RP-coloured discs + optional PM arrows (`showPmVectors`); new `↗ PM vectors` toggle. New `ColorMagnitudeDiagram.svelte` — SVG scatter (x=BP−RP, y=M_G/apparent-G inverted) of REAL sources, click a point ⇄ selects the sky marker/table row (shared `selectedCatalogIndex`); honest empty state for all-NaN. Tests: `gaiaViz.test.ts` (colour monotonic + NaN→grey; ϖ→pc; M_G hand value; PM endpoint scales/null) + `ColorMagnitudeDiagram.test.ts` (one circle per finite source, click→onSelect(index), empty state) + Playwright `gaia-rich.spec.ts` (markers paint >1 colour, CMD points, CMD click links the sky marker + row; PM-vector arrows appear only when toggled). |
