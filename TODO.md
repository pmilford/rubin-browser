# Rubin Browser — Canonical TODO

Single source of truth for actionable items. **IDs are globally unique and never
reused or renumbered** — reference an item by its ID (e.g. "do 100–104"). Detailed
rationale lives in `BACKLOG.md`; this file is the numbered index + status.

Status: `TODO` · `WIP` (in progress) · `DONE` · `BLOCKED` (needs a decision — see note).

## Active batch (being implemented now)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| 100 | Plain-language glossary — hover tooltips + "?" panel | DONE | glossary section + filter in HelpModal; title= tooltips on DP1/Filter labels; module `glossary.ts` (44 terms) + HelpModal glossary tests |
| 101 | Catalog overlay + linked table (Gaia / Rubin Object via TAP) | DONE | Gaia DR3 overlay (public, no token): markers on the canvas + CatalogTable (row⇄marker select, recenter on click) over gaia.ts→catalog.ts; CatalogTable presentational + Playwright wiring test. Rubin Object source is a follow-up (needs tap.ts, now fixed). |
| 102 | MOC / DP1 footprint coverage layer (shade where data exists) | DONE | ImageViewer.renderCoverage() projects footprint.ts discs on the main canvas; ⊙ DP1 coverage toggle; Playwright paint/clear outcome test |
| 103 | Simbad "what's here?" (right-click) + name resolve | DONE | right-click → onSkyContext → objectsNear → SimbadPanel; public (no auth), works without a token; presentational + page.route Playwright tests |
| 104 | Click-to-copy coordinates (sexagesimal ⟷ decimal) | DONE | StatusBar: click RA/Dec to copy, format toggle, copy-pair (decimal); 4 tests |
| 105 | Grid coordinate-system toggle (equatorial ↔ galactic ↔ ecliptic) | DONE | graticuleLines generalized (equatorial path unchanged) via coords.ts; formatGridLabel (l/b, λ/β); System selector; iso-line + label unit tests + Playwright repaint test |
| 106 | PNG screenshot export of the current view | DONE | ImageViewer.exportPng() composites stacked canvases → toBlob download; ⤓ PNG button; Playwright download-outcome test |
| 107 | Magnifier loupe + whole-sky locator inset | DONE | loupe canvas magnifies pixels under the cursor (locator inset already existed as .fov-minimap); 🔍 Loupe toggle; Playwright tracks-cursor outcome test |
| 108 | Offline image-diff display (epoch A vs B → transients) | DONE | DiffPanel: A/B/diff (diverging) canvases + transient list over the tested offlineIntensityFrame→differenceImages(median)→detectTransients pipeline; ⧉ Diff toggle (offline only); ground-truth unit test + presentational + Playwright wiring test |
| 109 | FITS cutout display pipeline (SODA→FITS→WCS readout) | DONE | End-to-end: soda.ts (ObsTAP/DataLink discovery + SODA sync fetch) → readFits → wcs.ts (pure TAN/CD pixel↔sky) → cutoutRender.ts (percentile-clip→scaling→colormap, NaN→transparent) → CutoutPanel.svelte (canvas + scale/colormap/invert + live pixel-value & RA/Dec readout), wired into TileViewer via a 🔬 Cutout toggle with explicit idle/loading/loaded/error state (thrown errors shown verbatim, never a blank panel). Tested: wcs (18) + cutoutRender (6) + CutoutPanel (6) unit; Playwright cutout.spec routes a synthetic FITS + one ObsCore row and asserts non-black canvas + plausible RA/Dec, plus a zero-rows "No DP1 … covers" error-path. Scoped out: only linear TAN/CD WCS (no SIP/TPV distortion); the LIVE SODA path needs a real RSP token so it stays unverified in CI (endpoint + DataLink-ID recipe are documented assumptions in soda.ts/obscore.ts). |

## Backlog (not yet called out to implement)

| ID | Item | Status | Notes |
|----|------|--------|-------|
| 120 | RGB band-mixing composite (ugrizy → R/G/B, per-channel stretch, Lupton) | TODO | big #2; needs FITS pixels (109) |
| 121 | DS9 region files (draw / import / export circles·ellipses·polygons) | TODO | big #3 |
| 122 | Aperture / radial-profile photometry (click → profile, curve-of-growth) | TODO | big #5; calibrated once 109 lands |
| 123 | Object-type classification FROM the image under the cursor | TODO | BACKLOG #10 (PRD ready, measured accuracy required) |
| 124 | Time-varying intensity across multi-epoch cubes (multi-scale, sub-pixel) | TODO | BACKLOG #2 |
| 125 | Zoom/fetch perf tail: off-thread `createImageBitmap` decode, request cancellation, higher-res allsky | MOSTLY DONE | Shipped: **in-flight DEDUP** (`tileScheduler.ts` — a tile requested twice before it resolves hits the network once), **request CANCELLATION** (superseded batches abort in-flight loads — `AbortController` for the auth fetch path, detach handlers + `src=''` for `<img>`; a cancelled tile re-fetches when it reappears; an abort never counts as a tile failure / never trips auto-fallback), and the **higher-res HiPS Allsky backdrop** (fetch the single `Norder3/Allsky.<ext>` preview instead of 48 order-1 tiles; slice its 768 order-3 tiles — IVOA packing `n_tiles_in_row=int(sqrt(768))=27`, `divmod(ipix,27)` — into a SEPARATE backdrop map drawn as Pass 0, so it can't block the sharp same-order load; graceful fallback to the order-1 enumeration on 404/taint). Tests: `tileScheduler.test.ts` (dedup + supersede), `perf-hud.spec.ts` (queue-stranding regression), `allsky-backdrop.spec.ts` (single-Allsky request + falsifiable NPIX-to-centre placement + 404→order-1 fallback). **Deferred: off-thread `createImageBitmap` decode** (tiles still decode on the main thread via `<img>`; moving to a worker + `ImageBitmap` is the remaining piece). |
| 126 | Real-data TAP regression fixtures (`tests/regression/` + `tests/fixtures/`) | TODO | referenced by test scripts, not built |
| 127 | Cross-platform polish + PWA (pinch-zoom, responsive breakpoints, manifest/SW) | TODO | BACKLOG #8 |
| 128 | `tap.ts` DP0.2→DP1 namespace reconciliation | TODO | blocks live Rubin Object cone search / "Fetch details"; unblocks 101's Object source |
| 129 | DP1 dataset discovery: fetch `/api/hips/v2/dp1/list` instead of hardcoding datasets | TODO | BACKLOG B-a remainder |
| 130 | Gravitational-lens catalog overlay | DONE | Bundled, web-verified (SIMBAD J2000 + discovery papers) set of 19 famous strong lenses spanning all 4 types (`lenses.ts`→`lensCatalogSet()`→feature-101 `CatalogSet`). New INDEPENDENT ImageViewer layer (`renderLensCatalog`, gold labelled diamond markers) that coexists with the Gaia cyan layer (no silent conflict); ⬦ Lenses toggle + linked CatalogTable; row click recenters + reports name/type/z. Fixed a sign error in the Einstein Cross anchor (Dec is +3.3585, not −3.3586). Tests: unit `lenses.test.ts` asserts 5 anchors by name (fails on zeros/sign-flip); Playwright `lens-catalog.spec.ts` asserts markers paint + view recenters onto the lens. |
| 132 | Performance monitoring HUD + instrumentation | DONE | Pure `utils/perfMetrics.ts` (`PerfMetrics`): counters (tiles requested / fetched-from-network / served-from-cache / in-flight / errors), cache **hit-rate** = hits/(hits+misses) with a 0/0 guard, rolling min/max/avg windows for per-tile load time and per-frame render time (→ FPS), injectable clock. Wired into `ImageViewer`'s REAL fetch path (start/end/cache-hit/cancel/error) and render path (each full `render()` timed). New default-OFF `PerfHud.svelte` overlay (`aria-label="Performance HUD"`, stable per-field aria-labels) toggled from `TileViewer` (⏱ Perf), fed a throttled live snapshot via `onPerfSnapshot`. Fixed a real leak: the mount `$effect` cleanup aborted in-flight loads via `tileScheduler.clear()` without reconciling the perf gauge, so `in-flight` never returned to 0 on an effect re-run — added `abortInFlightLoads()` (clear + `recordFetchCancel` per aborted load). Tests: adversarial `perfMetrics.test.ts` (hit-rate formula + 0/0, rolling window bounding, FPS from render times, counter exactness — a constant stub fails); Playwright `perf-hud.spec.ts` (HUD toggles, metrics non-zero + sane, in-flight→0 at idle, cache hit-rate RISES on pan-away-and-back). |
| 131 | Gaia rich visualization (colour/PM/parallax + linked CMD) | DONE | Surfaces the rich Gaia columns `gaia.ts` already fetched but the feature-101 overlay threw away, at zero new API cost. New pure `utils/gaiaViz.ts` (verified conventions: `pmra`=μα*=μα·cosδ; M_G=G+5·log10(ϖ_mas)−10 for ϖ>0; d_pc=1000/ϖ; blue→white→red BP−RP ramp, NaN→grey). `catalog.ts` `gaiaToCatalogSet` now carries optional `colorRgb`/`pmRaMasYr`/`pmDecMasYr` + a "Distance (pc)" column (— for ϖ≤0). `ImageViewer.renderCatalog` paints filled BP−RP-coloured discs + optional PM arrows (`showPmVectors`); new `↗ PM vectors` toggle. New `ColorMagnitudeDiagram.svelte` — SVG scatter (x=BP−RP, y=M_G/apparent-G inverted) of REAL sources, click a point ⇄ selects the sky marker/table row (shared `selectedCatalogIndex`); honest empty state for all-NaN. Tests: `gaiaViz.test.ts` (colour monotonic + NaN→grey; ϖ→pc; M_G hand value; PM endpoint scales/null) + `ColorMagnitudeDiagram.test.ts` (one circle per finite source, click→onSelect(index), empty state) + Playwright `gaia-rich.spec.ts` (markers paint >1 colour, CMD points, CMD click links the sky marker + row; PM-vector arrows appear only when toggled). |
